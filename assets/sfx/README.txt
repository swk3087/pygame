SFX is disabled by design.

Only background music is used:
- `bgm.ogg` (recommended)
- fallback: `bgm.mp3` or `bgm.wav`

Place one of those files in this folder and the game will auto-play it in a loop.
